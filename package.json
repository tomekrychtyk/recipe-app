{
  "name": "food-recipe-app",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^20.17.25",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
